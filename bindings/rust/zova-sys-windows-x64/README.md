# zova-sys-windows-x64

Generated C for `x86_64-pc-windows-msvc`. Cargo selects this source-only package through
`zova-sys`; applications should depend on `zova`. This package performs no
compilation or downloads. See `native/metadata.json` for source provenance.
